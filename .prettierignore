node_modules
dist
coverage
**/dist
**/coverage
pnpm-lock.yaml
# Scaffold templates contain {{PLACEHOLDER}} tokens, not valid TS/JSON until generated.
# The EMITTED tree is formatted instead, by scripts/scaffold-parser.mjs after substitution: this
# line is why nothing here can be checked, and why every scaffolded parser was once born red.
scripts/parser-template
# PERF-P0 calibration datasets: machine-written evidence, formatted by the experiment not by us.
experiments/perf-calibration/data
# @cosyte/process fixture consumers: input to the real tools, not source. One of them is
# DELIBERATELY unformatted (it is how `format` and `format --check` are proven to do anything at
# all), so formatting them here would delete the evidence.
packages/process/test/fixtures
